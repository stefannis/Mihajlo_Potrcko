@using Mihajlo_Potrcko.Models
@model Mihajlo_Potrcko.Models.ViewDataContainer

@{
    ViewBag.Title = "Index
    Zaposleni pageModel = Model.PageModel;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Korisnik.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zaposleni2.JMBG)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Korisnik.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zaposleni2.JMBG)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ZaposleniID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ZaposleniID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ZaposleniID })
        </td>
    </tr>
}

</table>
