@model IEnumerable<Mihajlo_Potrcko.Models.Slika>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Link)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Link)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SlikaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SlikaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SlikaId })
        </td>
    </tr>
}

</table>
