@using Mihajlo_Potrcko.Models
@model Mihajlo_Potrcko.Components.ViewDataContainer

@{
    ViewBag.Title = "Index";
    IEnumerable<Nasa_banka> pageModel = Model.PageModel;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => pageModel.First().Stanje_racuna)
        </th>
        <th>
            @Html.DisplayNameFor(model => pageModel.First().Poslednja_uplata)
        </th>
        <th></th>
    </tr>

@foreach (var item in pageModel) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Stanje_racuna)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Poslednja_uplata)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Broj_racunaNB }) |
            @Html.ActionLink("Details", "Details", new { id=item.Broj_racunaNB }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Broj_racunaNB })
        </td>
    </tr>
}

</table>
