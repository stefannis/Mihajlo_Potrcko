@model IEnumerable<Mihajlo_Potrcko.Models.Vozac>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nalog.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zaposleni.FK_JMBG)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nalog.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zaposleni.FK_JMBG)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VozacID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VozacID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VozacID })
        </td>
    </tr>
}

</table>
