@using Mihajlo_Potrcko.Models
@model Mihajlo_Potrcko.Components.ViewDataContainer

@{
    ViewBag.Title = "Reklamacija";
    Reklamacija pageModel = Model.PageModel;
    if (pageModel == null)
    {
        pageModel = new Reklamacija();
    }

    <h2>Izmeni reklamaciju</h2>
    Html.RenderPartial("_PostForm", pageModel);
}