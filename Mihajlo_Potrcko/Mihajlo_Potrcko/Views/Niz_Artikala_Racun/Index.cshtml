@using Mihajlo_Potrcko.Models
@model Mihajlo_Potrcko.Components.ViewDataContainer

@{
    ViewBag.Title = "Index";
    IEnumerable<Niz_Artikala_Racun> pageModel = Model.PageModel;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => pageModel.First().Artikal.Naziv_artikla)
        </th>
        <th>
            @Html.DisplayNameFor(model => pageModel.First().Racun.Adresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => pageModel.First().Kolicina)
        </th>
        <th></th>
    </tr>

@foreach (var item in pageModel) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artikal.Naziv_artikla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Racun.Adresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kolicina)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Niz_artikla_racunID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Niz_artikla_racunID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Niz_artikla_racunID })
        </td>
    </tr>
}

</table>
