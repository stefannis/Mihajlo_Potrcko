@using Mihajlo_Potrcko.Models
@model Mihajlo_Potrcko.Models.ViewDataContainer

@{
    ViewBag.Title = "Index";
    Racun pageModel = Model.PageModel;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kupac.JMBG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vozac.VozacID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Datum_izdavanja)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Iznos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adresa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kupac.JMBG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vozac.VozacID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Datum_izdavanja)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Iznos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adresa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RacunID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RacunID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RacunID })
        </td>
    </tr>
}

</table>
