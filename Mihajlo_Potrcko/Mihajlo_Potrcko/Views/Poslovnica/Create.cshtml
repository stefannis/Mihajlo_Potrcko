@using Mihajlo_Potrcko.Models
@model Mihajlo_Potrcko.Components.ViewDataContainer

@{
    ViewBag.Title = "Poslovnica";
    Poslovnica pageModel = Model.PageModel;
    if (pageModel == null)
    {
        pageModel = new Poslovnica();
    }
    var db = new Potrcko();

    <h2>Dodaj novu poslovnicu</h2>
    Html.RenderPartial("_PostForm", pageModel);
}
