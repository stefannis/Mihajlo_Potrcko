@model IEnumerable<Mihajlo_Potrcko.Models.Kupac>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Korisnik.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nalog.Username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Korisnik.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nalog.Username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KupacID }) |
            @Html.ActionLink("Details", "Details", new { id=item.KupacID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KupacID })
        </td>
    </tr>
}

</table>
